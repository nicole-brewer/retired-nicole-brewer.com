# live reload (blogdown)

options(
  blogdown.ext = '.Rmd',
  blogdown.author = 'admin',
  blogdown.yaml.empty = TRUE,
  blogdown.new_bundle = TRUE,
  blogdown.method = 'html'
)
print("Sourcing local Rprofile")
